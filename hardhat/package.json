{
  "name": "nexus-arb-hardhat",
  "version": "2.0.0",
  "description": "Hardhat deployment and testing for NexusFlashReceiver",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:fork": "hardhat test --network hardhat",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network arbitrum",
    "verify": "hardhat verify --network arbitrum"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.20.0",
    "dotenv": "^16.4.1"
  }
}
